{
  "name": "zotero-submission-tracker",
  "version": "0.1.17",
  "private": true,
  "type": "module",
  "description": "Local-only submission tracking for Zotero 8, 9, and 10",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
