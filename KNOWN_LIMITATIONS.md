# 已知限制 / Known Limitations

1. 看板当前由独立的 Zotero 内部窗口承载，而不是 Zotero 主窗口的内容标签。入口、数据和业务逻辑均已隔离在 `src/host.ts`，后续若 Zotero 提供稳定公开的内容标签接口，可只替换宿主层。
2. 所有状态均由用户手动更新；不登录、不抓取 ScholarOne、Editorial Manager、OJS 或其他平台。
3. 跟进日期不会根据审稿周期自动推算，也不会产生操作系统通知、邮件或短信。
4. JSON 恢复会先备份当前数据，然后完整替换；不支持合并导入。CSV 不能用于恢复。
5. 没有云同步、团队协作、移动端或投稿文件版本管理。
6. Zotero 8 / 9 / 10（10.0.1 稳定版，2026-08-24 发布）均已通过本地验收；manifest 的 strict_max_version 为 10.0.*，后续大版本需重新测试并上调。
7. 自动更新清单托管于 GitHub；GitHub 不可访问时，Zotero 无法检查或下载插件更新，但本地投稿管理不受影响。
8. 示例截图或 GIF 需要使用无真实账号的演示数据在 Zotero 图形环境中录制，当前仓库暂未提供。
